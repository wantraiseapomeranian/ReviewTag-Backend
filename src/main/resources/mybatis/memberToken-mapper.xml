<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="memberToken">

	<!-- 등록 -->
	<insert id="insert">
		insert into member_token(
			member_token_no, member_token_target, member_token_value
		)
		values(member_token_seq.nextval, #{memberTokenTarget}, #{memberTokenValue})
	</insert>
	
	<!-- 상세조회 -->
	<select id="detail" resultType="memberDto">
		select * from member_token
		where
			member_token_target = #{memberTokenTarget}
			and
			member_token_value = #{memberTokenValue}
	</select> 

	
	<!-- 삭제 -->
	<delete id="delete">
		delete member_token where member_token_no = #{memberTokenNo}
	</delete>
	
	<delete id="deleteByTarget">
		delete member_token where member_token_target = #{memberTokenTarget}
	</delete>
	
</mapper>