<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
 <mapper namespace="reviewLike">
 
 	<insert id="insert">
 		insert into review_like(member_id, review_no) values(#{memberId}, #{reviewNo})
 	</insert>
 	
 	<select id="countForCheck" resultType="long">
 		select count(*) from review_like where member_id = #{memberId} and review_no = #{reviewNo}
 	</select>
 	
 	<select id="countByReviewNo" resultType="long">
 		select count(*) from review_like where review_no = #{reviewNo}
 	</select>
 	
 	<select id="countByMemberId" resultType="long">
 		select count(*) from review_like where member_id = #{memberId}
 	</select>
 	
 	<select id="selectListByMemberId" resultType="long">
 		select review_no from review_like where member_id = #{memberId}
 	</select>
 	
 	<delete id="delete">
 		delete review_like where member_id = #{memberId} and review_no = #{reviewNo}
 	</delete>
 	
	<!-- 좋아요 신뢰도 기능 -->
 	<select id="countTotalLikeByWriter" resultType="int">
 		select count(*) from review_like A join review B on A.review_no = B.review_no
 		where B.review_writer = #{writer}
 	</select>
 	
 
 </mapper>